/* import { datePicker } from './date-picker.styles';

export type DatePickerPortalProps = {
	id: string;
};
export const DatePickerPortal = ({ id }: DatePickerPortalProps) => <div id={id} css={datePicker} />;
 */
